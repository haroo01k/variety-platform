# AGENTS.md

이 문서는 이 저장소에서 작업하는 모든 AI 코딩 에이전트(Claude, Codex 등)가
공통으로 따라야 할 작업 규칙입니다. 특정 AI에 종속되지 않는 일반적인
규칙만 담습니다.

## 작업 전

1. [PROJECT.md](PROJECT.md)를 읽고 현재 개발 단계와 포함/제외 기능
   범위를 확인합니다.
2. `git status`로 현재 브랜치 상태와 반영되지 않은 변경 사항을
   확인합니다.
3. 요청받은 범위가 PROJECT.md의 "포함 기능" 범위를 벗어나면,
   먼저 사용자에게 확인하거나 범위를 명확히 하고 진행합니다.

## 코드 변경 원칙

1. 기존 구현과 관계없는 파일을 삭제하거나 전면 재작성하지 않습니다.
   변경이 필요하면 최소 범위로 수정합니다.
2. 공통 기능, 농업인 기능, 소비자 기능은 서로 다른 폴더로 분리합니다.
   - 공통 UI/유틸: `src/components/common`, `src/lib`
   - 농업인 전용 컴포넌트: `src/components/farmer`
   - 소비자 전용 컴포넌트: `src/components/consumer`
   - 화면(라우트) 단위 페이지: `src/pages` (농업인/소비자 화면은
     `src/pages/farmer`, `src/pages/consumer`로 하위 분리)
   - 화면 전반에서 쓰는 mock 데이터: `src/data`
   - 타입 정의: `src/types`
   - 전역 스타일/디자인 토큰: `src/index.css` (Tailwind CSS 사용,
     `@theme`에 브랜드 색상 토큰 정의)
3. 하나의 파일(App.tsx 등)에 모든 화면·로직을 몰아넣지 않습니다.
   기능 단위로 파일을 분리합니다.
4. 특정 AI 도구에 의존하는 코드 구조, 주석, 네이밍을 만들지 않습니다.
   (예: 특정 도구 전용 설정 파일 경로를 코드에 하드코딩하지 않기)

## 보안 / 환경변수

1. 환경변수와 비밀키(API 키, 토큰 등)를 소스코드에 직접 작성하지
   않습니다. `.env`, `.env.local`을 사용하고, 두 파일 모두
   `.gitignore`에 포함되어 있어야 합니다.
2. Supabase의 `service_role` 키를 포함한 관리자 권한 키는 절대
   프런트엔드 코드나 클라이언트 번들에 포함하지 않습니다.
   프런트엔드에는 공개 가능한 anon/public 키만 사용합니다.

## 패키지 관리

1. 새 패키지를 추가할 때는 어떤 목적으로 필요한지(어떤 문제를
   해결하는지) 작업 결과에 설명합니다.
2. 이미 설치된 패키지로 해결 가능한 경우 새 패키지를 추가하지
   않습니다.

## 작업 완료 전 확인

1. 작업을 마치기 전에 반드시 `npm run build`를 실행해서 타입/빌드
   오류가 없는지 확인합니다.
2. 빌드가 실패하면 원인을 해결한 뒤 다시 빌드를 확인합니다.

## 작업 후 보고

작업이 끝나면 다음 내용을 정리해서 알려줍니다.

1. 변경하거나 추가한 파일 목록
2. 빌드/실행 확인 결과
3. 다음에 이어서 할 작업(있다면)

## Git

1. 사용자가 명시적으로 요청하지 않으면 `git commit`을 실행하지
   않습니다. (스테이징까지는 가능하지만 커밋은 사용자 요청 시에만)
2. 강제 push, 히스토리 재작성(`git rebase -i`, `git push --force` 등)은
   사용자가 명확히 요청한 경우에만 수행합니다.
