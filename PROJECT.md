# PROJECT.md

## 서비스 목적

"기후농사" — 기후 변화에 강한 신품종(작물 품종)을 농업인과 소비자에게
각각 맞는 방식으로 추천·홍보하는 반응형 웹 서비스입니다.

- 농업인에게는 지역, 작물, 기후위험, 재배 목적에 따른 신품종 탐색 기능을
  제공합니다.
- 소비자에게는 작물별 취향 테스트와 신품종 카드, "먹어보고 싶어요"
  기능을 제공합니다.

## 사용자

| 구분 | 설명 |
| --- | --- |
| 농업인 | 지역/작물/농업 문제(기후위험)/재배 목적을 입력하면 분석 과정을 거쳐 추천 품종과 점수를 확인 |
| 소비자 | 관심 작물을 선택하면 취향 유형과 신품종 카드를 확인하고 반응(예정)을 남김 |

## 대상 작물

감자, 옥수수, 복숭아, 사과, 배추 (5종)

작물 목록은 [src/data/crops.ts](src/data/crops.ts)에 정의되어 있습니다.
품종 원본 데이터(25개 품종, 기후위험 근거 포함)는
[src/data/varieties.ts](src/data/varieties.ts)에 있으며, 감자/옥수수/
복숭아/사과/배추 통합 데이터(2026-09-21 확정본)를 그대로 옮긴 것입니다.

## 포함 기능 (2단계·3단계 완료 — 실제 추천 로직 연동)

- 공통 시작 화면: 서비스명 "기후농사", 메인 문구, "농업인이세요?" /
  "소비자세요?" 선택 카드
- 농업인 조건 입력 화면 (`/farmer`): 작물 선택 + F1~F7(지역·기상위험·
  관수·수확시기·용도·병해충·관리우선순위) 질문, 사과·복숭아는 FA1~FA3
  (겨울 최저기온·작업 부담·유통 조건) 질문 추가. 제출 시
  [src/lib/farmerScoring.ts](src/lib/farmerScoring.ts)로 실제 점수를
  계산합니다.
- 농업인 분석 화면 (`/farmer/analyzing`): 계산된 결과를 잠시 보여준 뒤
  결과 화면으로 이동
- 농업인 추천 결과 화면 (`/farmer/result`): 상위 3개 품종, 품종별 점수·
  추천 이유 2개·주의사항(작물공통 근거 안내 등)·출처, 조건 충돌로 제외된
  품종 안내
- 소비자 작물 선택 화면 (`/consumer`): 감자/옥수수/복숭아/사과/배추 카드
- 소비자 취향 테스트 화면 (`/consumer/:cropId`): C1~C5(취향유형·활용·맛·
  식감·새로움) 질문, 사과·복숭아는 C6~C7(먹고 싶은 시기·보관 방법) 질문
  추가. 제출 시 [src/lib/consumerScoring.ts](src/lib/consumerScoring.ts)로
  실제 점수를 계산합니다.
- 소비자 추천 결과 화면 (`/consumer/:cropId/result`): 상위 3개 품종 카드
  (캐릭터, 취향 유형명, 맛·식감·향 설명, 활용 레시피, 출처),
  "먹어보고 싶어요"/"카드 공유하기" 버튼(자리만 있음, 실제 저장·공유
  API는 아직 없음)
- 점수 로직은 `Claude_업로드용_추천질문지_및_점수로직.pdf`의 규칙(직접
  근거 1.0배·간접근거 0.5배·작물공통 0배, 기후점수 ±3 제한, 동률 시
  품종 특이 직접근거 수 → 지역 직접 일치 → 출처 완결성 → 품종ID
  오름차순)을 그대로 따르며, T01~T10 테스트 시나리오로 검증했습니다.
- Tailwind CSS 기반 모바일 우선 반응형 레이아웃 (데스크톱은 최대 너비
  + 중앙 정렬)
- 이미지가 없는 자리는 모두 교체하기 쉬운 placeholder(색상 도형 +
  이모지, 품종별 캐릭터색 HEX 반영)로 구성
- 소비자 "좋아요" 기능(4단계 일부 선반영): 로그인 없이 기기별 익명
  ID로 Supabase에 좋아요를 기록·집계 ([supabase/schema.sql](supabase/schema.sql),
  [src/lib/likes.ts](src/lib/likes.ts)). 농업인 결과 화면에는 같은 작물의
  소비자 관심 TOP3를 보여주는 읽기 전용 코너([ConsumerInterestSection](src/components/farmer/ConsumerInterestSection.tsx))가
  있고, 좋아요는 추천 점수·순위에 전혀 반영되지 않습니다. `/admin`에서
  작물·품종별 누적 좋아요 수를 조회할 수 있습니다(수정·삭제 기능 없음,
  로그인 보호 없음 — 아래 "제외 기능" 참고).

## 제외 기능 (이후 단계에서 구현)

- "먹어보고 싶어요" 저장, 실제 카드 공유 API
- 로그인/인증 (좋아요 기능은 예외적으로 비로그인 익명 방식으로 4단계보다
  먼저 구현했지만, `/admin` 화면 자체의 접근 제한은 아직 없음 — 실제
  배포 전 인증 적용 필요)
- 데이터베이스(Supabase 등) 연동 중 사용자 응답 저장은 좋아요 외에는
  아직 없음(취향 테스트 응답 자체를 저장하지는 않음)
- PWA(오프라인 지원, 설치, manifest/service worker) 적용
- 배포
- F6(병해충)·F7(관리우선순위)·FA2/FA3(과수 작업·유통)의 세부 매칭은
  자유 서술 텍스트 키워드 기반 근사치이며, T01~T10 시나리오가 다루지
  않는 조합이라 정밀도는 향후 데이터 보강 시 개선 대상입니다.

## 개발 단계

1. **1단계 (완료)** — 공통 시작 화면 + 농업인/소비자 전체 화면 흐름의
   UI와 라우팅 뼈대 구성.
2. **2단계 (완료)** — 농업인 경로: 실제 추천 로직/데이터 연동
   ([src/lib/farmerScoring.ts](src/lib/farmerScoring.ts)).
3. **3단계 (완료)** — 소비자 경로: 실제 취향 테스트 로직
   ([src/lib/consumerScoring.ts](src/lib/consumerScoring.ts)). "먹어보고
   싶어요"/카드 공유 상호작용은 버튼 자리만 있고 저장·공유 기능은 4단계
   이후로 남겨둠.
4. **4단계** — 데이터베이스(Supabase 등) 연동: 사용자 응답·반응 저장,
   인증(필요 시) 적용.
5. **5단계** — PWA 적용(설치, 오프라인 지원), 성능/접근성 점검, 배포.

각 단계 진행 전 이 문서를 먼저 확인하고, 현재 단계에 해당하지 않는
기능을 앞서 구현하지 않습니다.
