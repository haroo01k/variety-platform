# PROJECT.md

## 서비스 목적

"기후농사" — 기후 변화에 강한 신품종(작물 품종)을 농업인과 소비자에게
각각 맞는 방식으로 추천·홍보하는 반응형 웹 서비스입니다.

- 농업인에게는 지역, 작물, 기후위험, 재배 목적에 따른 신품종 탐색 기능을
  제공합니다.
- 소비자에게는 작물별 취향 테스트와 신품종 카드, "먹어보고 싶어요"
  기능을 제공합니다.

## 사용자

| 구분 | 설명 |
| --- | --- |
| 농업인 | 지역/작물/농업 문제(기후위험)/재배 목적을 입력하면 분석 과정을 거쳐 추천 품종과 점수를 확인 |
| 소비자 | 관심 작물을 선택하면 취향 유형과 신품종 카드를 확인하고 반응(예정)을 남김 |

## 대상 작물

감자, 옥수수, 복숭아, 사과, 배추 (5종)

작물 목록은 [src/data/crops.ts](src/data/crops.ts)에 임시 데이터로
정의되어 있습니다. 실제 데이터 연동 전까지는 이 파일을 기준으로 삼습니다.

## 포함 기능 (현재 단계 — 화면 UI + 화면 이동)

- 공통 시작 화면: 서비스명 "기후농사", 메인 문구, "농업인이세요?" /
  "소비자세요?" 선택 카드
- 농업인 조건 입력 화면 (`/farmer`): 지역/작물/농업 문제/재배 목적
  드롭다운 + "신품종 추천받기" 버튼
- 농업인 분석 화면 (`/farmer/analyzing`): 분석 중 안내 + 병해충·기후위험
  정보 카드, 잠시 후(또는 버튼으로) 결과 화면 이동
- 농업인 추천 결과 화면 (`/farmer/result`): 추천 품종, 지역 적합성/기후
  대응성/재배 목적/병해충 특성/소비자 관심도, "추천 근거 보기"/"다른
  품종 비교" 버튼
- 소비자 작물 선택 화면 (`/consumer`): 감자/옥수수/복숭아/사과/배추 카드
- 소비자 추천 카드 화면 (`/consumer/:cropId`): 품종 캐릭터, 취향 유형명,
  맛·식감·향 설명, 활용 요리, "먹어보고 싶어요"/"카드 공유하기" 버튼
- Tailwind CSS 기반 모바일 우선 반응형 레이아웃 (데스크톱은 최대 너비
  + 중앙 정렬)
- 이미지가 없는 자리는 모두 교체하기 쉬운 placeholder(색상 도형 +
  이모지)로 구성

> 위 화면들은 모두 `src/data`의 mock 데이터로 채워져 있으며, 실제
> 추천/분석 로직은 아직 없습니다. 조건 선택 → 분석 → 결과, 작물 선택 →
> 추천 카드로 이어지는 **화면 흐름과 레이아웃**을 우선 구현한 단계입니다.

## 제외 기능 (이후 단계에서 구현)

- 지역/작물/기후위험/재배 목적 기반 실제 추천 알고리즘
- 실제 취향 테스트 로직 (현재는 작물 선택 → 고정 mock 카드만 표시)
- "먹어보고 싶어요" 저장, 실제 카드 공유 API
- 로그인/인증
- 데이터베이스(Supabase 등) 연동, 실제 데이터 저장
- PWA(오프라인 지원, 설치, manifest/service worker) 적용
- 실제 품종/지역/기후 데이터 (현재는 `src/data`의 임시 데이터만 사용)
- 배포

## 개발 단계

1. **1단계 (현재)** — 공통 시작 화면 + 농업인/소비자 전체 화면 흐름의
   UI와 라우팅 뼈대 구성 (mock 데이터, 실제 로직 없음).
2. **2단계** — 농업인 경로: 실제 추천 로직/데이터 연동, "추천 근거
   보기"/"다른 품종 비교" 상세 화면 구현.
3. **3단계** — 소비자 경로: 실제 취향 테스트 로직, "먹어보고 싶어요"
   상호작용, 카드 공유 기능 구현.
4. **4단계** — 데이터베이스(Supabase 등) 연동: 실제 품종 데이터, 사용자
   응답 저장, 인증(필요 시) 적용.
5. **5단계** — PWA 적용(설치, 오프라인 지원), 성능/접근성 점검, 배포.

각 단계 진행 전 이 문서를 먼저 확인하고, 현재 단계에 해당하지 않는
기능을 앞서 구현하지 않습니다.
